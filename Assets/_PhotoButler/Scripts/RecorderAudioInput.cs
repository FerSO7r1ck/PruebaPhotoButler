using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RecorderAudioInput : MonoBehaviour
{
    //void OnAudioFilterRead(float[] sampleBuffer, int channelCount)
    //{
    //    if (RecorderManager.Instance.IsRecording() && RecorderManager.Instance.IsRecordAudioEnable())
    //    {
    //        RecorderManager.Instance.CommitAudioSample(sampleBuffer, channelCount);
    //    }
    //}
}
